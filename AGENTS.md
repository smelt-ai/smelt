# AGENTS.md

## 工作原则

- **多质疑自己**：不要默认自己是对的，动手前验证假设，实现后主动检查边界与反例。
- **模糊需求先澄清真实目标**：仅当需求明显模糊、关键信息缺失且不同答案会实质改变实现方向时，先不要直接回答或动手。应先简明指出：（1）用户未明确说出但当前推理依赖的默认假设；（2）仍缺少的关键信息及其可能如何改变答案；（3）处理这类问题最常见的一个错误。然后只提出一个最关键的问题并等待用户回答，再给出最终答案或实施方案。信息充分、可安全验证，或能通过读取代码自行确定的事项不得借此反问用户。
- **根治问题，不要叠补丁**：修复先写能复现的失败用例，再定位根因。确认方向或结构有问题时及时止损重构，不要打补丁硬撑。改动范围保持最小，但必须覆盖源头。
- **正确性与最佳实践优先，不为兼容性妥协**：以业内最佳实践和清晰、正确的设计为准，不为保留错误、过时或不合理的行为编写兼容代码。对就是对，错就是错；该迁移就迁移，该删除就删除，不做折中式兼容。
- **持久化数据使用 SQLite，不使用 JSON**：业务数据统一存入 SQLite，不以 JSON 文件充当数据库或持久化存储；JSON 仅用于配置、接口传输或临时数据交换。
- **不要钻牛角尖**：某个方向卡住时，先停下来评估投入产出，不要无限深挖；及时换角度、求助或先绕开，必要时记 TODO。
- **改动前先理解原意**：修改代码前，先搞清楚原有实现为什么这样设计、考虑了什么约束（历史原因、兼容性、性能、边界场景等），避免破坏原有意图。
- **评估改动影响面**：每次改动前明确影响范围——被哪些模块/调用方依赖、是否有外部依赖与约定、行为变更会波及哪些场景；改动后确认覆盖相关调用方与回归场景。
- **不要过度设计**：只针对当前真实需求做最小足够的设计，不为假想的未来场景提前抽象、分层或引入新框架。复杂度必须被当下约束证明。
- **功能应插件化，不要写死**：可扩展能力走插件/注册点/配置，不要把具体实现硬编码进核心路径。写死会把每次新增、替换、下线都变成改主干，抬高维护成本并放大回归面。
